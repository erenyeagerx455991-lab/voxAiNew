# Provider Budget Baseline Audit — V7.0.6

Generated by source inspection. No estimates. No assumptions.

## Function Call Sites

| Function | File | Line | Live? |
|---|---|---|---|
| `recordTokenUsage()` | `src/agents/llm/llmClient.ts` | 23 | YES — called after every successful Groq/OpenRouter response |
| `recordTokenUsage()` | `src/agents/llm/aiService.ts` | 59 | YES — called via `accountTokens()` after every successful OpenRouter response |
| `recordLLMCall()` | `src/agents/llm/llmClient.ts` | 68, 80, 120, 128, 148, 164 | YES — called on success and failure |
| `recordLLMCall()` | `src/agents/llm/aiService.ts` | 224, 248 | YES — called on success and failure |
| `checkProviderBudget()` | — | — | **DEAD — never called** |
| `recordProviderTokens()` | — | — | **DEAD — never called** |
| `recordProviderRequest()` | — | — | **DEAD — never called** |

## Definition Sites (for reference)

| Function | File | Line |
|---|---|---|
| `checkProviderBudget()` | `src/cost/providerBudget.ts` | 62 |
| `recordProviderTokens()` | `src/cost/providerBudget.ts` | 53 |
| `recordProviderRequest()` | `src/cost/providerBudget.ts` | 47 |
| `recordTokenUsage()` | `src/cost/tokenBudget.ts` | 60 |
| `recordLLMCall()` | `src/telemetry/tokenMetrics.ts` | 34 |

## Summary

`checkProviderBudget()` and `recordProviderTokens()` are fully implemented but completely disconnected from runtime.
RPM limits, TPM limits, and emergency shutdown cannot trigger because `checkProviderBudget()` is never called before requests.
`recordTokenUsage()` IS active, so daily/monthly budget accounting works — but the per-minute RPM/TPM windows in `providerBudget.ts` are never updated.
