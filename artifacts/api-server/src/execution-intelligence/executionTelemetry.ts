// ── V9.6 Execution Intelligence — Telemetry ───────────────────────────────────
// Thin re-export so the telemetry route imports a single symbol.
export { getExecutionIntelligenceSnapshot } from './executionMetrics.js';
