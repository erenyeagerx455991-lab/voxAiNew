// ── V9.7 Planning Intelligence — Telemetry ─────────────────────────────────────
// Thin re-export so the telemetry route imports a single symbol.
export { getPlanningMetricsSnapshot } from './planningMetrics.js';
