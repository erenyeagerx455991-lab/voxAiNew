NexoGen V7.0 — Scalability Foundation

Context

Current status:

- V6.4.6 Security Baseline complete
- V6.4.7 Test Infrastructure complete
- V6.4.8.1 Build Pipeline Extraction complete
- V6.4.9 Telemetry complete
- V6.4.9.2 Telemetry Correctness complete
- V6.6 Registry V2 + RAG complete
- 293/293 tests passing

Do NOT modify prompts, AI behavior, SSE event contracts, registry retrieval logic, telemetry schemas, or frontend UI.

Goal is scalability and reliability.

---

Phase 1 — Queue Architecture

Create:

src/queue/
├── buildQueue.ts
├── queueWorker.ts
├── queueTypes.ts
├── queueMetrics.ts

Requirements:

- BullMQ + Redis
- Build requests become jobs
- HTTP route returns jobId immediately
- Worker executes build pipeline
- Support retries
- Support cancellation
- Support timeout

---

Phase 2 — Build Worker Isolation

Move build execution out of HTTP request lifecycle.

Current:

HTTP Request
→ runBuildPipeline()

Target:

HTTP Request
→ enqueueBuild()
→ Worker
→ runBuildPipeline()

Requirements:

- Existing build pipeline unchanged
- Existing telemetry unchanged
- Existing repair system unchanged

---

Phase 3 — Workspace Lifecycle Manager

Create:

src/workspace/
├── workspaceManager.ts
├── workspaceCleanup.ts
├── workspaceRegistry.ts

Requirements:

- Track active workspaces
- Track last access time
- Auto cleanup expired workspaces
- Crash recovery
- Prevent orphan directories

---

Phase 4 — User Build Limits

Requirements:

- Per-user active build limit
- Per-user queue limit
- Daily build quota
- Daily token quota

Integrate with existing auth middleware.

---

Phase 5 — Cost Protection Layer

Create:

src/cost/
├── tokenBudget.ts
├── providerBudget.ts
├── budgetMetrics.ts

Requirements:

- Groq token tracking
- OpenRouter token tracking
- Daily limits
- Monthly limits
- Emergency shutdown threshold

---

Phase 6 — Horizontal Scaling Preparation

Requirements:

- No process-local build state
- No process-local queue state
- Shared Redis state
- Worker count configurable

Target:

1 API server
+
N workers

without code changes.

---

Phase 7 — Queue Telemetry

Add metrics:

- queued jobs
- active jobs
- completed jobs
- failed jobs
- average wait time
- p95 queue latency

Expose:

GET /api/telemetry/queue

Protected by auth middleware.

---

Phase 8 — Reliability Tests

Add:

tests/integration/
├── queueFlow.test.ts
├── workerRecovery.test.ts
├── workspaceCleanup.test.ts
├── buildQuota.test.ts

Requirements:

- Queue survives restart
- Worker retries work
- Cleanup works
- Quotas enforced

---

Phase 9 — Load Testing

Create:

scripts/load/
├── 10-users.ts
├── 100-users.ts
├── 500-users.ts

Measure:

- throughput
- latency
- queue depth
- memory usage

Generate report.

---

Phase 10 — Final Validation

Success criteria:

- 293 existing tests still pass
- New tests pass
- Queue telemetry works
- Worker recovery verified
- No SSE contract changes
- No AI behavior changes
- No prompt changes

Generate:

scalabilityReport.md

Include:

- before vs after architecture
- max concurrent builds
- queue throughput
- worker throughput
- scalability bottlenecks
- remaining V7.x roadmap

IMPORTANT:

Do not move monoliths into new files.
Every module must remain under 300 LOC.
Keep orchestrators thin.
No placeholder implementations.
All metrics must be real measured values.
Report actual numbers, never estimated numbers.