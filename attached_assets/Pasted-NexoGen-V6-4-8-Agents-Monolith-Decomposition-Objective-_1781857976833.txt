NexoGen V6.4.8 — Agents Monolith Decomposition

Objective

Refactor "api-server/src/routes/agents.ts" (≈4,748 LOC) into focused domain modules without changing external behavior.

This is an architecture refactor only.

DO NOT:

- Change UI
- Change API contracts
- Change SSE event formats
- Change Runtime Builder behavior
- Change Repair Engine behavior
- Change RequestManager integration
- Change Security Baseline (V6.4.6)
- Change Test Infrastructure (V6.4.7)

All existing tests must continue passing.

---

Phase 1 — Architecture Audit

Before changing code:

Generate a dependency map of agents.ts.

Identify:

- Planner Agent
- Architecture Agent
- Frontend Code Generator
- CodeFix Agent
- DNA Mixer
- Runtime Builder integration
- Repair Engine
- SSE Streaming
- Context Compression
- Session State

Output:

agents-decomposition-report.md

Include:

- responsibilities
- imports
- exported functions
- internal dependencies

No code changes yet.

---

Phase 2 — Create Shared Types Layer

Create:

src/agents/types.ts

Move all reusable types from agents.ts:

Examples:

- AgentMessage
- BuildContext
- PlannerResult
- ArchitectureResult
- RuntimeResult
- DNAProfile
- SSEEvent

Requirements:

- Remove duplicated interfaces
- No any types
- Strict TypeScript

Goal:

agents.ts imports shared types instead of declaring them inline.

---

Phase 3 — Extract Planner Module

Create:

src/agents/planner/plannerAgent.ts

Move:

- planner prompt construction
- planner response parsing
- planner validation
- planner retry logic

Export:

generatePlan()

Input/output contract must remain identical.

No route logic allowed.

---

Phase 4 — Extract Architecture Module

Create:

src/agents/architecture/architectureAgent.ts

Move:

- architecture generation
- architecture validation
- architecture scoring
- architecture retries

Export:

generateArchitecture()

Preserve all current behavior.

---

Phase 5 — Extract Frontend Generation Module

Create:

src/agents/frontend/frontendAgent.ts

Move:

- code generation prompts
- frontend generation
- component generation
- file generation

Export:

generateFrontend()

Requirements:

Keep:

- RequestManager usage
- model selection
- token budgeting

unchanged.

---

Phase 6 — Extract CodeFix Module

Create:

src/agents/frontend/codeFixAgent.ts

Move:

- code repair prompts
- import/export fixes
- syntax fixes
- formatting cleanup

Export:

fixGeneratedCode()

No logic changes.

---

Phase 7 — Extract DNA Engine

Create:

src/agents/dna/dnaAgent.ts

Move:

- DNA mixer
- style weighting
- personality blending
- diversity selection

Export:

buildDNAProfile()

Requirements:

Preserve current output scoring.

No behavioral changes.

---

Phase 8 — Extract Repair Engine

Create:

src/agents/repair/repairAgent.ts

Move:

- repair loops
- repair pass tracking
- targeted file repair
- autonomous builder repair integration

Export:

runRepairLoop()

Must continue using:

RequestManager

for all model calls.

---

Phase 9 — Extract SSE Layer

Create:

src/agents/streaming/sseManager.ts

Move:

- SSE event creation
- event formatting
- stream lifecycle
- progress updates
- completion events

Export:

createSSEManager()

Requirements:

All existing frontend SSE consumers must continue working unchanged.

Event names must remain identical.

---

Phase 10 — Extract Context Management

Create:

src/agents/context/contextBuilder.ts

Move:

- context compression
- registry injection
- token trimming
- file selection
- prompt assembly

Export:

buildAgentContext()

Requirements:

No token-budget regressions.

---

Phase 11 — Thin Route Layer

After extraction:

agents.ts should become a thin orchestrator only.

Target size:

< 1000 LOC

Preferred:

< 500 LOC

Responsibilities allowed:

- request validation
- authentication
- route orchestration
- SSE startup
- calling extracted modules

Nothing else.

---

Phase 12 — Type Safety Cleanup

Remove unsafe patterns from extracted modules:

Replace:

- any
- as any
- unknown as

where possible.

Goal:

Reduce unsafe usages in agents subsystem by at least 50%.

Do not disable TypeScript checks.

---

Phase 13 — Observability Integration

Replace raw console logs inside extracted modules.

Use:

structured logger

Track:

- planner duration
- architecture duration
- generation duration
- repair duration
- total build duration

No UI changes.

---

Phase 14 — Validation

Run:

npm test

Requirements:

129/129 tests must pass.

Coverage thresholds must remain:

- Statements ≥ 70%
- Branches ≥ 60%
- Functions ≥ 70%
- Lines ≥ 70%

No regressions allowed.

---

Success Criteria

✓ agents.ts reduced below 1000 LOC

✓ Planner extracted

✓ Architecture extracted

✓ Frontend generator extracted

✓ CodeFix extracted

✓ DNA engine extracted

✓ Repair engine extracted

✓ SSE manager extracted

✓ Context builder extracted

✓ No API contract changes

✓ No UI changes

✓ No security regression

✓ 129/129 tests passing

✓ Coverage gates still passing

---

Deliverables

Provide final report:

- Original LOC
- New LOC
- Modules created
- Imports reduced
- Unsafe types removed
- Test results
- Coverage results
- Remaining technical debt