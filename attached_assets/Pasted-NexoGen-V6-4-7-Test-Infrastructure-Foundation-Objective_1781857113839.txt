NexoGen V6.4.7 — Test Infrastructure Foundation

Objective

Establish the first real automated testing foundation for NexoGen.

Current audit result:

Testing Score: 4/100

Current state:

- Zero unit tests
- Zero integration tests
- Zero CI verification
- Refactoring agents.ts is unsafe
- Security changes cannot be regression tested
- Runtime changes cannot be verified automatically

Goal:

Create a production-grade testing foundation before any major refactor.

No UI changes.

No architecture changes.

No runtime behavior changes.

Testing infrastructure only.

---

Phase 1 — Install Test Stack

Install:

- vitest
- @vitest/coverage-v8
- supertest
- @types/supertest

Configure:

vitest.config.ts

Requirements:

- TypeScript support
- Node environment
- Coverage reporting
- Fast execution

Validation:

npm test

must run successfully.

---

Phase 2 — Test Directory Structure

Create:

src/tests/

Structure:

src/tests/
├── unit/
├── integration/
├── fixtures/
├── helpers/
└── setup.ts

Requirements:

Clear separation between:

- Unit tests
- Integration tests
- Shared helpers

---

Phase 3 — packageScanner Tests

Target:

runtime/security/packageScanner.ts

Create:

packageScanner.test.ts

Minimum coverage:

Safe package.json

Blocked scripts:

- preinstall
- postinstall
- prepare
- prepublish

Suspicious dependencies

Path traversal detection

Validation:

All scanner rules verified.

---

Phase 4 — errorClassifier Tests

Target:

runtime/errorClassifier.ts

Create:

errorClassifier.test.ts

Coverage:

npm dependency conflict

404 package not found

TypeScript compile errors

Rollup import errors

JSX syntax errors

Unknown build failure

Validation:

Correct classification returned.

---

Phase 5 — dependencyResolverV2 Tests

Target:

runtime/dependencyResolverV2.ts

Coverage:

Import graph generation

Hook detection

Component detection

Route detection

AST resolver integration

Fallback behavior

Validation:

Graph output matches expectations.

---

Phase 6 — buildRepairTargets Tests

Target:

runtime/buildExecutor.ts

Coverage:

Single failing file

Multiple failing files

Context trimming

Related import selection

Token protection

Validation:

Correct repair context generated.

---

Phase 7 — Security Middleware Integration Tests

Targets:

authMiddleware
rateLimiter
corsConfig

Use:

supertest

Coverage:

Valid API key

Invalid API key

Missing API key

Allowed origin

Blocked origin

Rate limit exceeded

Validation:

Correct HTTP responses.

---

Phase 8 — Workspace Cleanup Tests

Target:

workspaceCleanup.ts

Coverage:

Active workspace

Building workspace

Repairing workspace

Completed workspace

Expired workspace

Validation:

Only safe workspaces deleted.

---

Phase 9 — Regression Protection

Create:

criticalRegression.test.ts

Verify:

- npm install still uses --ignore-scripts
- package scanner still active
- path traversal protection still active
- auth middleware still registered
- rate limiting still registered

Purpose:

Prevent accidental removal of security controls.

---

Phase 10 — Coverage Gates

Coverage targets:

Statements: 70%

Functions: 70%

Branches: 60%

Lines: 70%

Fail build if below thresholds.

---

Phase 11 — CI Test Command

Add scripts:

test
test:watch
test:coverage

Requirements:

Single command:

npm test

runs entire suite.

---

Phase 12 — Validation Run

Execute:

All tests

Generate report:

- Total tests
- Passed tests
- Failed tests
- Coverage %
- Slowest tests
- Runtime

Success Criteria

✓ Vitest configured

✓ Unit tests operational

✓ Integration tests operational

✓ Security layer covered

✓ Runtime layer covered

✓ Coverage enforcement active

✓ npm test works

✓ No application behavior changes

Final Report

Generate:

- Number of tests added
- Coverage percentage
- Critical paths protected
- Regression checks added
- Remaining uncovered modules