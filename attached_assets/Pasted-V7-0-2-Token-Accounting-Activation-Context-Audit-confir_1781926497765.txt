V7.0.2 — Token Accounting Activation

Context

Audit confirmed:

- recordTokenUsage() exists
- recordTokensUsed() exists
- budget gate exists
- dailyTokenQuota exists

But none are connected to live LLM traffic.

Result:

- global token budget always reports 0
- per-user token quota always reports 0
- budget shutdown never triggers naturally

Goal

Activate token accounting using real provider usage data.

Do NOT modify UI.

Do NOT modify queue architecture.

Do NOT modify telemetry schemas.

Do NOT estimate tokens.

Use provider-reported values only.

==================================================
PHASE 1 — TRACE ALL LLM ENTRY POINTS

Audit:

- callGroq()
- callOpenRouter()
- streaming paths
- retry paths
- fallback paths

Document:

tokenFlowBaseline.md

Answer:

1. Where do tokens enter the system?
2. Where are usage values available?
3. Which providers expose usage metadata?
4. Which paths do not expose usage?
5. Which paths retry automatically?

==================================================
PHASE 2 — IDENTIFY CANONICAL TOKEN SOURCE

For each provider:

Groq
OpenRouter

Determine:

- prompt_tokens
- completion_tokens
- total_tokens

availability

Document exact response fields.

No assumptions.

==================================================
PHASE 3 — DOUBLE COUNT PROTECTION

Audit:

retry logic
fallback logic

Verify:

A failed request must not count twice.

Rules:

Count only completed provider responses.

If provider returns usage twice through retries:

record once.

==================================================
PHASE 4 — GLOBAL TOKEN ACCOUNTING

Wire:

recordTokenUsage()

from canonical provider usage source.

Requirements:

Every successful provider response increments:

- prompt tokens
- completion tokens
- total tokens

==================================================
PHASE 5 — PER USER TOKEN ACCOUNTING

Wire:

recordTokensUsed()

Requirements:

Associate usage with:

- userId
- buildId
- requestId

Verify:

dailyTokenQuota can actually trigger.

==================================================
PHASE 6 — STREAMING VALIDATION

Verify:

Streaming responses still produce usage metrics.

If usage unavailable:

Document limitation.

Do not invent estimates.

==================================================
PHASE 7 — BUDGET GATE VALIDATION

Demonstrate:

1. Usage recorded
2. Budget rises
3. Threshold reached
4. Gate activates

Must use real accounting path.

==================================================
PHASE 8 — REGRESSION TESTS

Add tests:

tokenAccounting.test.ts
budgetGate.test.ts
providerUsageParsing.test.ts

Verify:

- retries not double counted
- fallbacks not double counted
- streaming paths handled correctly
- quota enforcement works

==================================================
PHASE 9 — AUDIT REPORT

Generate:

tokenAccountingReport.md

Include:

Before:

global usage = dead
per-user usage = dead

After:

global usage = live
per-user usage = live

Provide:

real file names
real line numbers
real usage fields

==================================================
FINAL QUESTIONS

Answer:

1. Can token budgets now trigger?
2. Can daily user token quotas now trigger?
3. Are retries counted once?
4. Are fallback providers counted once?
5. Are streaming requests accounted for?
6. Which requests still cannot be measured?

Do not claim 100% coverage unless verified.

Report measured reality only.